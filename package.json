{
  "name": "react-css-vars",
  "description": "--Element-foo: (props) => fooify(props.bar)",
  "version": "1.0.0",
  "main": "dist/react-css-vars.js",
  "scripts": {
    "start": "babel lib -wsd build",
    "dist": "babel --minified lib -d dist"
  },
  "peerDependencies": {
    "react": "16.x",
    "react-dom": "16.x"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1"
  }
}
